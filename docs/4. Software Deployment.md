# Software Deployment

## CI

[`.github/workflows/ci.yml`](../.github/workflows/ci.yml):

- `cargo test --workspace --all-features`
- `cargo llvm-cov` with **90%** line fail-under
- Coverage log artifact upload

## Container

- [`Dockerfile`](../Dockerfile)
- Compose service `wallet-service` — ports `8083`, `50051`
- Volume `wallet-rust-data` for local SQLite fallback if configured

## Runtime env (compose)

- `DATABASE_URL`, `PORT`, `GRPC_PORT`
- `RABBITMQ_URL`, `BIDMART_EVENTS_EXCHANGE`
- `AUCTION_SERVICE_BASE_URL` for reconciliation hooks

## Observability

- Prometheus job `bidmart-wallet-service` → `/metrics`
- Dashboard reference: infrastructure Grafana provisioning folder
